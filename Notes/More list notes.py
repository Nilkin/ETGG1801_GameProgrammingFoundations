#More list notes 11/2/15

#bullet_list=[]#creates an empty list.
#asteroid_list=[]#creates another empty list.
#Code organization:
    #important stuff
    #initialize game variables.
#while True:
    #Get input/update player position
    #Compute and update other objects
    #check objects for collision
    #check objects for screen exit
    #Render screen
#clean up and exit.
#Go through every bullet in the bullet_list.
#for bullet in bullet_list:
    #(bx,by)=bullet
#go through each asteroid in asteroid_list.
#for asteroid in asteroid_list:
    #(ax,ay)=asteroid

#check for collision.
#if distance(bx,by,ax,ay)<=:
     #store asteroid/bullets to be deleted.
#bullet_list.append((_,_)) adds to the list
#asteroid_list.append((_,_)) adds to the list
#To add to a list:
    #bullet_list.append((_,_)) <- item to add.
#To remove an item from a list:
    #del bullet_list[i]
    #or
    #bullet_list.remove((_,_)) <- item to be removed.
